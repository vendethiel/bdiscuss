if currentUser()
  p You're logged in as #{currentUser().get('username')}. 
    if isAdmin()
      span You're an admin.
else
  .login-form-container

  p You're logged off. 
    a.login-button(href="/login") Login in 
    | or 
    a(href="/register") Register.